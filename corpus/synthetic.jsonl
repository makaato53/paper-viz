{"desc": "Plot $\\sin(x)$ from 0 to $2\\pi$", "code": "from manimlib.imports import *\nclass SinPlot(Scene):\n    def construct(self):\n        axes = Axes(x_length=6, y_length=3)\n        graph = axes.get_graph(lambda x: np.sin(x), [0, 2*PI])\n        self.play(ShowCreation(axes), ShowCreation(graph))\n        self.wait(1)"}
{"desc": "Display the equation $E=mc^2$", "code": "from manimlib.imports import *\nclass EquationScene(Scene):\n    def construct(self):\n        eq = TexMobject(r\"E=mc^2\")\n        self.play(Write(eq))\n        self.wait(1)"}
